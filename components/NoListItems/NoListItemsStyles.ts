import { StyleSheet } from "react-native";


const NoListItemsStyles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        margin: 20,
        justifyContent: 'center',
        alignItems: 'center'
    }, 
    text: {
        fontSize: 20,

    }
})

export {NoListItemsStyles}